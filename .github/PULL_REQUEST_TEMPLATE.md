**What**
One or two sentences on what this changes and why. Link the issue: Fixes #

**Component**
daemon / controller / CLI / tracker / installer / keeper / SDK / docs

**How it was tested**
- [ ] `go vet ./...`
- [ ] `go test -short ./...`
- [ ] tracker tests against PostgreSQL (`DATABASE_URL` set), if the tracker changed
- [ ] built and ran the installer on Windows / macOS, if the installer changed

**Notes for the reviewer**
Anything that needs a closer look, follow up work, or a migration.
